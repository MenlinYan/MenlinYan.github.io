<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Menlin Yan | Academic Homepage</title>

    <!-- Inter 字体 -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-color: #050505;
            --card-bg: #0e0e0e;
            --text-main: #e8e8e8;
            --text-muted: #8b8b8b;
            --accent-color: #00eaff; 
            --accent-hover: #bc13fe; 
            --font-main: 'Inter', sans-serif;
        }

        body {
            margin: 0;
            font-family: var(--font-main);
            background: var(--bg-color);
            color: var(--text-main);
            line-height: 1.7;
            -webkit-font-smoothing: antialiased;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        header {
            text-align: center;
            margin-bottom: 60px;
        }

        /* Avatar */
        .avatar {
            width: 130px;
            height: 130px;
            border-radius: 50%;
            border: 3px solid var(--accent-color);
            margin-bottom: 20px;
            box-shadow: 0 0 20px rgba(0, 234, 255, 0.4);
            object-fit: cover;
        }

        h1 {
            font-size: 2.8rem;
            margin: 10px 0;
            font-weight: 800;
            background: linear-gradient(120deg, #fff, var(--accent-color));
            -webkit-text-fill-color: transparent;
            -webkit-background-clip: text;
        }

        .subtitle {
            color: var(--text-muted);
            font-size: 1.05rem;
        }

        /* Navigation */
        nav {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 60px;
        }

        nav a {
            color: var(--text-muted);
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
            position: relative;
        }

        nav a:hover {
            color: var(--accent-color);
            text-shadow: 0 0 8px var(--accent-color);
        }

        section {
            margin-bottom: 70px;
            animation: fadeIn 1s ease;
        }

        h2 {
            font-size: 1.6rem;
            margin-bottom: 25px;
            border-left: 4px solid var(--accent-color);
            padding-left: 15px;
            color: #fff;
        }

        /* Paper Card */
        .paper-item {
            background: var(--card-bg);
            padding: 20px;
            border: 1px solid #1a1a1a;
            border-radius: 10px;
            margin-bottom: 22px;
            transition: 0.25s;
        }

        .paper-item:hover {
            border-color: var(--accent-color);
            transform: translateY(-3px);
            box-shadow: 0 0 20px rgba(0, 234, 255, 0.15);
        }

        .paper-title {
            font-size: 1.05rem;
            font-weight: 700;
        }

        .paper-authors {
            font-size: 0.92rem;
            color: var(--text-muted);
            margin-top: 5px;
        }

        .paper-venue {
            color: var(--accent-color);
            margin-top: 5px;
            font-size: 0.95rem;
            font-weight: 600;
        }

        .links a {
            display: inline-block;
            margin-right: 10px;
            margin-top: 10px;
            padding: 3px 8px;
            border-radius: 4px;
            font-size: 0.85rem;
            border: 1px solid #333;
            text-decoration: none;
            color: var(--text-main);
            transition: 0.25s;
        }

        .links a:hover {
            background: var(--accent-color);
            color: #000;
            border-color: var(--accent-color);
        }

        /* News timeline */
        .news-item {
            display: flex;
            margin-bottom: 12px;
        }

        .news-date {
            min-width: 110px;
            color: var(--accent-color);
            font-family: monospace;
        }

        footer {
            text-align: center;
            font-size: 0.8rem;
            color: #555;
            margin-top: 80px;
            padding-bottom: 40px;
        }

        /* Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
<div class="container">
    <header>
        <!-- 修复后的头像 URL -->
        <img src="https://avatars.githubusercontent.com/MenlinYan" class="avatar" />

        <h1>Menlin Yan</h1>
        <div class="subtitle">Ph.D. Student @ University Name</div>
        <div class="subtitle" style="font-size:0.95rem; margin-top:6px;">
            Research Interests: AI, Computer Vision, Cool Systems
        </div>
    </header>

    <nav>
        <a href="#about">About</a>
        <a href="#news">News</a>
        <a href="#publications">Publications</a>
        <a href="#contact">Contact</a>
    </nav>

    <section id="about">
        <h2>About Me</h2>
        <p>
            I am <strong>Menlin Yan</strong>, a Ph.D. student at [University], advised by Prof. [Name].
            My research focuses on <strong>Machine Learning</strong>, <strong>Computer Vision</strong>, and building cool intelligent systems.
        </p>
    </section>

    <section id="news">
        <h2>News</h2>
        <div class="news-item">
            <span class="news-date">[2025.11]</span>
            <span>One paper accepted to CVPR 2026!</span>
        </div>
        <div class="news-item">
            <span class="news-date">[2025.08]</span>
            <span>Started internship at Google Research.</span>
        </div>
        <div class="news-item">
            <span class="news-date">[2025.01]</span>
            <span>Built my new academic homepage.</span>
        </div>
    </section>

    <section id="publications">
        <h2>Selected Publications</h2>

        <div class="paper-item">
            <div class="paper-title">Your Awesome Paper Title Goes Here</div>
            <div class="paper-authors"><strong>Menlin Yan</strong>, Co-author One, Co-author Two</div>
            <div class="paper-venue">CVPR 2025 (Oral)</div>
            <div class="links">
                <a href="#">PDF</a>
                <a href="#">Code</a>
                <a href="#">Project</a>
            </div>
        </div>

        <div class="paper-item">
            <div class="paper-title">Another Great Work on Cool Topics</div>
            <div class="paper-authors">Co-author, <strong>Menlin Yan*</strong></div>
            <div class="paper-venue">NeurIPS 2024</div>
            <div class="links">
                <a href="#">PDF</a>
                <a href="#">Code</a>
            </div>
        </div>
    </section>

    <section id="contact">
        <h2>Contact</h2>
        <p>Email: menlin.yan [at] university.edu</p>
        <p>Github: <a href="https://github.com/MenlinYan" style="color:var(--accent-color);">@MenlinYan</a></p>
    </section>

    <footer>&copy; 2025 Menlin Yan. Designed with Cyberpunk Dark Mode.</footer>
</div>
</body>
</html>
